请完成以下多步骤任务，过程中注意记录关键信息到记事本：

【步骤 1 - 探索项目结构】
分析 src/mini_agent/ 目录下的子模块，找出所有包含 .py 文件的子目录，统计每个子目录的 .py 文件数量。

【步骤 2 - 深度分析】
从步骤 1 的结果中，找出 .py 文件数量最多的前 3 个子目录，分别读取它们下面的 __init__.py 或主要模块文件，分析这些模块各自负责的核心功能（用一句话概括）。

【步骤 3 - 记录关键发现】
在分析过程中，请把以下信息记录到记事本中：
- 每个子目录的文件数量和核心功能概要
- 你发现的任何值得注意的代码模式或架构特点
- 如果某个模块的 __init__.py 导出了大量子模块，记录下来

【步骤 4 - 整理与总结】
完成分析后：
- 列出当前记事本中的所有条目
- 将相关的条目合并为一条总结性记录
- 删除任何过时或不必要的条目

【步骤 5 - 生成报告】
基于你的分析和记事本中的记录，生成一份项目结构分析报告，保存到 ./test_result/project_structure_analysis.md。

---

【触发 notepad 功能的关键检查点】

1. notepad_add — 在步骤 2/3 中，agent 应该为每个发现的子目录添加条目
2. notepad_update — 如果后续分析修正了之前的结论，应该更新已有条目
3. notepad_remove — 步骤 4 中，agent 应该清理过时条目
4. notepad_list — 步骤 4 中，agent 应该列出所有条目以便整理
5. notepad_summarize — 步骤 4 中，agent 应该将多条相关条目合并为一条总结
6. 持久化验证 — 任务完成后，检查 .agent/sessions/<session_id>/notepad.json 是否存在且内容正确
7. system prompt 注入验证 — 在后续对话轮次中，记事本内容应出现在 system prompt 中
8. 配置开关验证 — 如果 notepad_enabled=false，工具调用应返回错误而非崩溃

---

【预期行为】

- 步骤 1 完成后，记事本中应有若干条关于子目录的条目
- 步骤 2 完成后，条目应包含核心功能描述
- 步骤 4 中，条目数量应减少（通过 summarize 合并 + remove 清理）
- 步骤 5 生成的报告内容应与记事本中的记录一致
- notepad.json 文件应包含最终合并后的条目