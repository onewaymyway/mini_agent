# 敏感令牌配置模板 —— 这个文件本身会被跟踪进版本库（供参考模板用），
# 但真正生效的 `secrets.local.yaml`（去掉 .example 后缀）已经被
# `.gitignore` 排除，不会被提交，可以放心填真实值。
#
# 用法：
#   cp config/secrets.local.yaml.example config/secrets.local.yaml
#   然后编辑 secrets.local.yaml，把下面的占位值换成真实值。

# 问财（iwencai）hexin-v 令牌。screener 功能报 401 时需要配置这个值，
# 见 stock_watch_continuous_improvement_plan.md 2026-08-27 变更记录。
# 获取方式（二选一）：
#   1. 自动：pip install websocket-client requests PyYAML（PyYAML 项目
#      本身已依赖，通常已装），然后跑
#      `python tools/fetch_iwencai_cookie.py`（默认连接你手动用
#      --remote-debugging-port=9222 打开的 Chrome；不想碰默认 profile
#      就加 --spawn 让脚本自己拉起一个独立实例）——按需完成登录/验证，
#      脚本自动检测到令牌后帮你写进这个文件（不需要手动编辑这一行）。
#   2. 手动：浏览器登录/访问 https://www.iwencai.com/ 后，F12 打开开发
#      者工具 → Application/存储 → Cookies，找到名叫 v 的 cookie，复制
#      它的值填在下面。
# 这个令牌会过期，过期后需要重新走一遍上面任一种方式更新，代码不会
# 自动刷新。
iwencai_cookie: ""
