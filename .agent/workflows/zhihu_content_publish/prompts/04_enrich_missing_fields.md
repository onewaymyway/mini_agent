# 缺失字段兜底提取 Prompt（python_step，仅在 DOM 选择器解析不到某些字段时使用）

下面是知乎问题详情页 `{url}` 抓取到的可见文本内容（已去除脚本/样式，可能有截断）：

```
{page_text}
```

## 任务

上面这个问题的大部分字段已经用页面结构（DOM 选择器）直接解析过了，但下面
列出的这几个字段还没有解析到（可能是页面本身缺这部分内容，也可能是没有
匹配到对应的 DOM 元素）。请你从上面的页面文本里补充提取，输出成 JSON，
**只需要包含下面列出的这几个字段**，提取不到的一律填 `null`，不要编造、
不要用占位符充数：

{missing_field_descriptions}

数字字段如果页面文本里写的是"1.2万"这类中文简写，换算成整数（1.2万 → 12000）。
