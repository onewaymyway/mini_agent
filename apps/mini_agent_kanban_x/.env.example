# 生产构建/预览时，若不通过 Vite dev proxy，可用此变量指定 API Base
# （默认是同源 /v1，适用于生产环境由 FastAPI 挂载静态资源的部署方式）
VITE_API_BASE=/v1

# 仅 dev 模式下 vite.config.ts 会读取，指向 mini-agent daemon 地址
VITE_DAEMON_TARGET=http://127.0.0.1:8765
