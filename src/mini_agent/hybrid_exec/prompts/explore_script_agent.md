你正在为下面这个任务探索一个可复用的 Python 脚本方案。与单轮问答不同，你现在可以使用工具（读文件、跑命令等）来先探查清楚任务涉及的数据/环境，再动手写脚本；如果一次探查不够，可以反复尝试和验证，直到确信这个脚本能稳定处理这类输入。

## 任务描述
{description}

## 本次输入数据样例（JSON）
{input_sample}

## 脚本协议（最终产出必须严格遵守）
1. 脚本必须定义一个函数 `run(ctx)`，这是唯一入口。
2. `ctx` 提供：
   - `ctx.input_json("task_input")` —— 获取本次任务的输入数据（结构即上面的输入样例）。
   - `ctx.params` —— dict，内容与输入数据相同。
   - `ctx.llm.ask(prompt, system="")` / `ctx.llm.ask_json(...)` —— 脚本运行时如需语言理解能力可调用，返回字符串/dict。
   - `ctx.write_output(filename, data)` —— 需要落盘中间产物时使用。
3. `run(ctx)` 返回值必须是字符串，或可 `json.dumps` 的 dict/list。
4. 遇到无法处理的输入应显式 `raise` 一个描述清楚的异常，不要吞掉错误或返回模糊默认值。

## 输出要求
探索/验证过程中你可以使用工具、输出中间思考。但**最后一轮回复**必须只包含最终定稿的脚本源码本身，不要有任何解释文字、不要用 markdown 代码块包裹（不要出现 ```python 或 ```）。
