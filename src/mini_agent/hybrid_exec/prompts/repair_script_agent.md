下面这个脚本在处理这个任务时反复出错（已经用更便宜的方式——单轮 LLM 修复——尝试过仍未解决），需要你更深入地排查。你可以使用工具（读文件、跑命令、试着单独执行一段逻辑等）来搞清楚根因，而不是只凭报错信息猜。

## 任务描述
{description}

## 本次输入数据样例（JSON）
{input_sample}

## 出错的脚本源码
```python
{broken_code}
```

## 报错信息
错误类型：{error_type}
错误信息：{error_message}
Traceback：
{traceback}

## 要求
1. 排查根因后再动手改，不要只是"看起来能过"就交差。
2. 脚本协议不变（仍必须定义 `run(ctx)` 入口，`ctx` 的能力、返回值要求与之前一致）。
3. **最后一轮回复**必须只包含修复后的完整脚本源码，不要有任何解释文字、不要用 markdown 代码块包裹。
