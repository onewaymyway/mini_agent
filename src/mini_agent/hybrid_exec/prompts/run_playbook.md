你正在参照一份已经验证过可行的"步骤说明"（playbook）完成下面这个任务。这份
playbook 是此前一次成功探索的产物，记录了大致的操作步骤——但页面/环境的具体
细节（选择器、文案、布局）可能和当时略有出入，你需要像一个照着 SOP 操作的人
一样，按 playbook 描述的整体思路去做，遇到细节不完全匹配时自行判断如何适应，
不要机械地要求每一步都逐字复现。

## 任务描述
{description}

## Playbook（参照执行）
{playbook}

## 本次输入数据（JSON）
{input_sample}

## 输出要求
可以使用工具完成任务。**最后一轮回复**必须只包含最终结果本身——如果结果是
结构化数据，输出可以被 `json.loads` 解析的 JSON；如果结果是自由文本，直接
输出文本本身。不要有任何解释性文字、不要用 markdown 代码块包裹。

如果照着 playbook 操作后发现整个方案在当前环境下走不通（不是细节出入，而是
根本性地行不通），如实说明"PLAYBOOK_INVALID: <原因>"，不要编造结果。
